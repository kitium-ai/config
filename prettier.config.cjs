/**
 * Shared Prettier settings for the KitiumAI monorepo.
 * Keep formatting opinions in one place so every package stays aligned.
 */
module.exports = {
  semi: true,
  singleQuote: true,
  trailingComma: 'es5',
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
  arrowParens: 'always',
};
